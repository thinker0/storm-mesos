java.library.path: "/usr/local/lib:native"
storm.messaging.transport: "backtype.storm.messaging.netty.Context"


# see if we need this
storm.messaging.transport: "backtype.storm.messaging.netty.Context"
storm.messaging.netty.server_worker_threads: 1
storm.messaging.netty.client_worker_threads: 1
storm.messaging.netty.buffer_size: 5242880
storm.messaging.netty.max_retries: 100
storm.messaging.netty.max_wait_ms: 1000
storm.messaging.netty.min_wait_ms: 100

mesos.master.url: "zk://localhost:2181/mesos"
mesos.executor.uri: "http://velum.io/pub/storm-mesos-0.9.tgz"
storm.zookeeper.servers:
    - "localhost"
nimbus.host: "localhost"

storm.local.dir: "storm-local"

mesos.framework.role: "*"
mesos.framework.checkpoint: false
#mesos.allowed.hosts:
#  - host1
#mesos.disallowed.hosts:
#  - host1
